services:
  brother-scan-web:
    build: .
    # Host networking allows SANE network discovery and direct access to LAN scanners.
    network_mode: host
    environment:
      SANE_DEVICE: "${SANE_DEVICE:-}"
      SCAN_TIMEOUT: "${SCAN_TIMEOUT:-120}"
    restart: unless-stopped
